version: '3.8'

services:
  # 1. Banco de Dados
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: metrics_db
    ports:
      - "5435:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U user -d metrics_db" ]
      interval: 10s
      timeout: 5s
      retries: 5

  # 2. Backend API
  backend:
    build: ./backend
    ports:
      - "5003:5003"
    volumes:
      - ./backend:/app
    environment:
      - FLASK_ENV=development
      - DATABASE_URL=postgresql://user:password@db:5432/metrics_db
      # - DATABASE_URL_SUPABASE=postgresql://postgres.klnntuqbigegopbmzcly:XNLVT3Lm8viPXK6f@aws-1-us-east-1.pooler.supabase.com:6543/postgres
      - PYTHONUNBUFFERED=1
      - GOOGLE_API_KEY=AIzaSyC-KhFAZF-7GtCfpjsnBV1r0bL9MxPmquQ
      - GEMINI_API_KEY=AIzaSyC-KhFAZF-7GtCfpjsnBV1r0bL9MxPmquQ
      - CLICKUP_API_KEY=pk_61029153_OZYMFRVK07NEKLIBQRINT2ZA5BLG3HBF
    depends_on:
      db:
        condition: service_healthy

  # 3. Frontend (React + Vite)
  frontend:
    build: ./frontend
    ports:
      - "5177:5177"
    volumes:
      - ./frontend:/app
      - /app/node_modules # Avoid overwriting container node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true # Better hot-reload
    depends_on:
      - backend

volumes:
  postgres_data:
